# Revision progress log

## 2026-09-24 — Step 1: Data provenance and reconciliation

**Work done.** Compared the local cleaned CSV with IFPRI's official BIHS Round 3 release and queried the official Harvard Dataverse metadata. Audited the local row count and `hhid2` uniqueness; checked the existing keyed-join audit; inventoried official documentation, identifier, and sampling-weight files; and documented climate and survey-design provenance limits in `paper/08_data_provenance_reconciliation.md`. No source data or model results were changed.

**Findings.** IFPRI reports 5,604 households and 325 PSUs. The local CSV has 5,605 rows: 5,604 distinct nonmissing `hhid2` values plus one unkeyed row at index 0, with no duplicated nonmissing IDs. The existing E0 surrogate key follows all 5,605 rows through downstream files but cannot authenticate the unkeyed row against IFPRI. The official Dataverse catalog contains the readme, questionnaire, codebooks, household identification module, and sampling-weight file. File downloads require a Dataverse guestbook response, so their contents were not recovered or used. Local `hhid2` semantics, PSU/stratum/weight mappings, climate and crop sources, merge keys, and measurement periods remain unverified.

**Status and next dependency.** Step 1's local and catalog audit is complete; definitive source-level reconciliation remains open. The researcher must access the official release through the guestbook and provide or authorize use of the relevant files before the extra row, survey design, and climate provenance can be adjudicated. The current 5,605-row dataset was retained without guessing an exclusion. Steps 2–6 have not been started.

**Sources.** [IFPRI release](https://bangladesh.ifpri.info/2021/06/bangladesh-integrated-household-survey-2018-19-third-round-dataset/); [BIHS DOI](https://doi.org/10.7910/DVN/NXKLZJ); [Dataverse metadata API](https://dataverse.harvard.edu/api/datasets/:persistentId/?persistentId=doi:10.7910/DVN/NXKLZJ); `paper/08_data_provenance_reconciliation.md`.

## 2026-09-24 — Step 2: Source synchronization and conflict resolution

**Work done.** Designated `paper/main.tex` as authoritative, synchronized `paper/07_paper_draft.md` with its abstract, methods, tables, figure sequence/captions, results, and limitations, and added a source-sync audit at `paper/09_source_sync_audit.md`. Checked all 16 Table I values, all 20 Table II values, each abstract number, and all three figure captions against the saved CSV/JSON outputs and rendered figures. Added the official IFPRI release citation to both versions and `paper/references.bib`. Rebuilt the five-page `paper/main.pdf` using `pdflatex`/`bibtex` successfully, with no unresolved citations or references.

**Changes and findings.** Corrected the Markdown's stale claim of four context plus 18 socioeconomic predictors to 16 context plus six plot predictors; corrected the bootstrap unit from households to 39 climate-profile groups; fixed two dimension-score ranges; aligned workflow, index-sensitivity, and model-performance figure numbering; and removed the Markdown-only cluster figure. Both versions now describe 11 LVI specifications as one baseline plus ten alternatives, identify the Gower comparison as a 1,500-household subsample, and describe the percentile bounds as sample-derived. Removed unverified “exogenous” wording and an invalid inference from overlapping marginal intervals. Explained why the pooled prevalence-baseline AUROC is 0.5122 although its mean within-fold AUROC is 0.5000. The paper now discloses the unresolved 5,605-versus-5,604 source-row discrepancy.

**Status.** Step 2 is complete. The source-level row and codebook questions from Step 1 remain open. Steps 3–6 have not been started.

## 2026-09-24 — Step 3: Research question and scope refinement

**Work done.** Retitled the LaTeX and synchronized Markdown paper as a composite-index leakage audit. Added an explicit research question about target construction and dependence-aware validation, and recast the contribution as an auditable sequence of target-lineage, related-record, and index-specification checks. Reordered the narrative emphasis in the abstract, introduction, experimental setup, and conclusion. Reduced clustering methods to a short secondary check and moved its results to a brief cautionary subsection after the supervised results.

**Changes and findings.** The main result is now the validity boundary of a constructed-label classifier, rather than a vulnerability typology. The remaining cluster paragraph reports the weak full-sample silhouette of 0.1521 and the contrast between same-pipeline bootstrap stability and geometry sensitivity; it explicitly declines a validated-type interpretation. The revised LaTeX built successfully as a five-page PDF. No new empirical result was claimed at this step; the controlled comparison is addressed in Step 5.

**Status.** Step 3 is complete. Step 4 is next; Steps 5–6 have not been started.

## 2026-09-24 — Step 4: Index coding and specification sensitivity

**Work done.** Recovered the published English BIHS Round 3 household questionnaire supplement and adjudicated its literacy and current-loan response codes against the implemented index. Documented the unresolved mapping from source questions to cleaned aggregates, the redundant flood fields, and the ambiguous zero-meal records in `paper/10_index_coding_adjudication.md`. Added a reproducible consensus analysis of the 11 saved E7 specifications (`outputs/stage5_required_experiments/e7_consensus_analysis.py` and `e7_consensus_summary.json`). Revised the LaTeX and Markdown methods, results, and abstract to describe household classifications as specification-dependent. Rebuilt the PDF successfully.

**Findings.** Questionnaire B1_08 codes literacy from cannot read/write (1) to can read/write (4), but the cleaned `adult_literacy_rate` aggregate and its denominator cannot be verified without raw member data and cleaning code. F02 codes current loan Yes=1 and No=2, so the binary recode is directionally consistent if the field maps to F02; whether a loan is protective or burdensome remains a substantive assumption. Flood occurrence and positive flood depth coincide in all 5,605 cleaned rows. Among the 11 chosen specifications, 708 records are always High, 2,543 never High, and 2,354 (42.0%) change status; 1,160 of the 1,868 baseline-High records are not always High. These correlated-specification counts measure descriptive agreement, not a probability of vulnerability or an adjudication of the correct coding.

**Status.** Step 4 is complete to the extent supported by the available questionnaire and saved artifacts. Official codebooks, raw modules, and cleaning code are still needed to certify the cleaned field mappings. Step 5 is next; Step 6 has not been started.

## 2026-09-24 — Step 5: Validation language and controlled leakage audit

**Work done.** Added `outputs/stage6_controlled_leakage_audit/controlled_audit.py` and saved its four-cell metric table, row-level out-of-fold predictions, and metadata. The diagnostic fixes the archived full-sample socioeconomic label and logistic model while crossing household-random versus reconstructed climate-profile-blocked five-fold splitting with 22 non-overlapping predictors versus those predictors plus 11 exact target ingredients. Updated both manuscript versions with its setup, results, and a distinct table. Clarified that the main E1 evaluation has **39 reconstructed climate-profile groups in five outer folds**, not 39 folds or verified spatial/district validation. Documented that its training-fold normalization and target-score cutoff vary (0.493580–0.502184), so pooled out-of-fold labels are fold-specific. Retained the E1 XGBoost values AUROC 0.6443, AUPRC 0.4479, and Brier 0.2103 against a prevalence-baseline Brier of 0.2226. The revised LaTeX compiled to a five-page PDF; final reference rerun is scheduled in Step 6.

**Findings.** In the fixed-label diagnostic, adding ingredients raises AUROC from 0.6313 to 0.9998 under household splitting and from 0.6085 to 0.9997 under climate-profile blocking; the target score reconstructs from these ingredients to below 1e-10 maximum absolute error. The four-cell results isolate ingredient inclusion within each split rule, but the full-sample target and normalization make them an intentional leakage stress test, not unbiased validation. Random-versus-blocked differences are descriptive because test folds change. The separate nested E1 result remains the internal performance estimate for a constructed, fold-specific label; neither result establishes external predictive validity.

**Status.** Step 5 is complete. Step 6 is next. Source-level provenance and independent-outcome validation remain open research dependencies.

## 2026-09-24 — Step 6: Scope curbing and final submission audit

**Work done.** Audited the authoritative LaTeX and synchronized Markdown for claims about early warning, causal climate effects, population prevalence, stable clusters, model superiority, and real-world predictive utility. Revised the introduction, results, limitations, and conclusion to state only internal methods findings for the cleaned sample. Replaced “registered” terminology that could imply preregistration with “saved” or “reported”; changed sample tertile counts to records rather than population prevalence. Added `paper/11_final_scope_audit.md` with final claim boundaries and artifact checks. Rebuilt `paper/main.pdf` and inspected the extracted PDF text, table placement, page count, and LaTeX warnings.

**Changes and findings.** The paper now presents the controlled experiment as an intentional leakage stress test, the E1 analysis as internal performance against fold-specific constructed labels, and clustering as a weak exploratory result (silhouette 0.1521). It explicitly requires an independent criterion outcome before assessing practical predictive utility. The conclusion no longer suggests operational early warning, deployment, or national inference. The new Table II matches all 12 rounded controlled-audit metrics; Table III matches all 20 rounded E1 metrics; the abstract's 42.0% specification-sensitive status matches the E7 consensus artifact. The final PDF is five A4 pages, with no undefined references or overfull boxes; only underfull spacing notices remain.

**Status.** Steps 3–6 are complete and logged in order. The manuscript is a bounded methods case study ready for author review, with source-level provenance and independent-outcome validation still open; it is not yet evidence for representative or operational claims. Step 1's 5,605-versus-5,604 row reconciliation remains unresolved until the official raw release and codebooks are accessible.

## 2026-09-24 — Extension 2, Task 1.1: Robust High membership and switch drivers

**Work done.** Created and ran `outputs/extension2_robust_high/robust_high.py` on all 11 saved E7 index variants. Saved a row-level `household_robustness.csv` with status under the variant set and each one-at-a-time switch flag, `variant_switch_drivers.csv` with High-to-non-High and non-High-to-High counts by coding choice, `switch_driver_patterns.csv` with exact per-record switch combinations, and `robust_high_summary.json`. Defined strict robust High as High in **all 11 observed** specifications; the 15 records omitted by the zero-meal-exclusion variant are not silently treated as non-High or certified robust.

**Findings.** Of 5,605 records, 5,590 have all 11 specifications and 15 have 10. Strict robust High contains **705** records; 3 additional incomplete records are High in all 10 available specifications. There are 2,543 never-High records and 2,354 switchers. The largest single-choice changes are omission of flood occurrence (1,210 switches; 605 losses/605 gains) and coding a loan as burden (1,146; 573/573). Exactly 967 records switch against baseline under only one alternative: 429 solely when flood occurrence is omitted, 397 solely when loan direction changes, 80 solely under literacy reversal, and smaller counts for other choices. These are one-at-a-time sensitivity attributions, not independent causal effects of the coding choices; the 11 variants are chosen and correlated, not exhaustive or probabilistic.

**Status.** Task 1.1 is complete. Task 1.2, excluded-outcome availability and validation, is next; no independent outcome result has yet been claimed.

## 2026-09-24 — Extension 2, Task 1.2: Excluded-outcome validation check

**Work done.** Audited all 137 local cleaned-data columns against the original LVI ingredients and the recovered BIHS questionnaire's food-consumption/food-security modules. Documented the candidate exclusions and non-eligibility in `paper/12_extension2_outcome_availability.md`. No outcome association or validation metric was computed because no suitable intentionally held-out criterion is present in the local file.

**Findings.** The cleaned data contain no FIES score/items, independently measured food-consumption or coping outcome, poverty transition, or later-wave outcome. Questionnaire modules O and X contain relevant source questions but are absent from this cleaned table. Meal frequency and income are already index ingredients; crop revenue may overlap upstream income construction and has unverified units; school fees are enrollment-dependent; `xxc_*` fields concern child-marriage law. None is documented as a prespecified validation outcome. The 705 strict robust-High records therefore remain a specification-consensus group, not an externally validated hardship group.

**Status.** Task 1.2 availability audit is complete; validation is data-limited and was not fabricated. Task 1.3, methodology and findings log consolidation, is next.

## 2026-09-24 — Extension 2, Task 1.3: Consolidated method and validation record

**Method.** The pipeline pivots the archived 11 E7 specifications by immutable local row index, checks one row per variant, marks High when `tertile_class == 2`, and keeps the zero-meal-exclusion variant's 15 absent rows as missing. “Strict robust High” requires High in all 11 specifications (5,590 complete records eligible); “High in all available” is reported separately. A switch driver is the named one-at-a-time variant whose High status differs from baseline. Row-level driver combinations are saved, so overlapping switches remain visible rather than being assigned to a single cause.

**All baseline comparisons.** High-status switches are: flood occurrence omitted 1,210; loan treated as burden 1,146; literacy reversed 686; loan omitted 602; equal indicator weights 598; 1st/99th-percentile normalization 418; literacy omitted 370; flood depth omitted 236; zero meals treated as missing 14; zero-meal households excluded 5 among 5,590 eligible records. Every complete variant retains an upper-tertile High count of 1,868, so gains and losses balance. The exclusion variant has 1,863 High among eligible records and five net gains relative to the eligible baseline comparison.

**Finding and validation boundary.** There are 705 strict robust-High, 2,543 never-High, and 2,354 switching records; three of the 15 incomplete records are High in their ten available specifications and remain uncertified as strict robust High. No intentionally excluded, separately measured food-security or welfare criterion exists in the cleaned data, so robustness cannot be externally validated here. The 11 choices are a selected correlated sensitivity set and do not identify a true vulnerability probability.

**Status.** Extension 2 Phase 1 is complete to the extent of the supplied artifacts. Phase 2 begins with the requested linking framework; no independent-outcome or transfer result has yet been produced.

## 2026-09-24 — Extension 3, Task 2.1: Independent-outcome and location-transfer framework

**Work done.** Wrote `paper/13_extension3_linking_framework.md` using the official BIHS Round 3 release, questionnaire, and Dataverse file catalog. Selected Round 3 Module X5's eight separately measured FIES responses as the prospective independent criterion and verified survey locations/PSUs as the transfer unit. Specified the official A identification modules, X5 files, codebooks, sampling-weight file, join cardinality checks, missing/refusal coding, interview and climate timing, split-household handling, and design-variable requirements. Prespecified simple outcome baselines and incremental index/robust-High comparisons, with a clear distinction between same-wave geographic transfer and future forecasting.

**Findings.** The questionnaire lists X5_01–X5_08 (Yes=1, No=2, Refused=3), but the local 137-column table lacks these items. The official catalog contains female X5-related files `111_bihs_r3_female_mod_x51.tab` and `112_bihs_r3_female_mod_x52.tab`, A identifier modules, and `158_r3_bihs_samplingweights.tab`; their contents and exact key/field mappings have not been verified because downloads require a guestbook response. The local 39 climate profiles cannot substitute for official PSU or location IDs. The unresolved extra local row and unknown climate dates are blocking join and timing checks.

**Status.** Task 2.1 is complete as a documented linking contract. Task 2.2, outcome-versus-baseline evaluation, is next and depends on obtaining the official source modules or a valid researcher-provided matched outcome file.

## 2026-09-24 — Extension 3, Task 2.2: Outcome-versus-baseline evaluation gate

**Work done.** Created `outputs/extension3_outcome_validation/evaluate_fies.py` to evaluate complete-response X5 affirmative counts on the same households under five-fold **verified-PSU** holdout. It compares the training-mean baseline, a prespecified demographic baseline, demographics plus continuous archived LVI, and demographics plus strict robust-High status; saves pooled MAE/RMSE/R², row-level out-of-fold predictions, and paired per-PSU MAE differences when valid input exists. The script requires a documented official household-key join, verified PSU, all eight X5 responses, source DOI/version, and a linkage-audit JSON. It excludes refusal/missing items rather than coding them as No and labels the archived full-sample index as transductive. Confirmed the script loads and rejects missing source data with an explicit error before producing any metric artifact.

**Current finding.** No official X5 response file, verified PSU mapping, or keyed matched outcome exists in the workspace. Consequently **no independent-outcome performance comparison was run or estimated**, and no practical predictive-validity claim can be made. The only numeric results still available are internal index-label audits from earlier steps; they are not substitutes for this evaluation.

**Status.** Task 2.2 implementation and data-availability gate are complete; empirical evaluation awaits the official files or a verified matched extract. Task 2.3, documenting the validation status, comparisons, and linking bottlenecks, is next.

## 2026-09-24 — Extension 3, Task 2.3: Validation status and data-linking record

**Work done.** Added `paper/14_extension3_validation_status.md` and consolidated the completed Task 2.1 join contract and Task 2.2 comparison design. The specified comparison uses the same verified-PSU-held-out households for the training-mean, demographic, demographic-plus-LVI, and demographic-plus-strict-robust-High models, with pooled MAE/RMSE/R² and per-PSU paired MAE differences. Confirmed that `evaluate_fies.py` produced no metric files when the required matched outcome and linkage audit were absent.

**Validation results and bottlenecks.** Official-key-matched X5 outcomes: **none in workspace**. Verified local-to-official household and PSU mapping: **none**. FIES count, outcome-baseline performance, incremental LVI/robust-High value, and location-transfer estimate: **not computable and not reported**. The official release lists 5,604 households/325 PSUs but the local table has 5,605 rows and one missing ID; official A, X5, codebook, and sampling-weight contents require a Dataverse guestbook response; climate reference dates remain unverified. A same-wave X5 comparison would test criterion association and held-out-location transfer, not future forecasting.

**Status.** All requested Extension 2 and Extension 3 tasks have been executed as far as the supplied data permit and logged in sequence. Extension 2 yields a reproducible strict robust-High group of 705 records and per-choice switch drivers. Empirical independent-outcome and transfer validation remains pending on official source files or a documented keyed extract; no paper claim of practical predictive validity is justified yet.

**Data handoff update.** The researcher indicated they can provide the official files. A subsequent workspace search found no X5, identification, codebook, sampling-weight, or matched-outcome file yet. The evaluation remains pending until those files are placed in the workspace; the completed pipeline will then perform the documented join checks and baseline comparison.

## 2026-09-24 — Extension 3 follow-up: Public welfare source and corrected key linkage

**Work done.** After the researcher said the files were unavailable, checked the public [World Bank reproducibility archive](https://reproducibility.worldbank.org/catalog/540). It contains BIHS Round 3 male identification, sampling weights, and a separately prepared household per-capita monthly expenditure aggregate, though it does not contain the female X5/FIES module. Preserved only the three required public source files in `data/extension3_public_source/`. Created and ran `outputs/extension3_outcome_validation/link_public_welfare.py`, which saves `public_welfare_link.csv` and an audit JSON with source hashes and row checks. The original cleaned CSV was not changed.

**Critical finding.** The local `hhid2` column is misordered relative to its other columns: **zero of 5,605** received IDs equal the official ID of the corresponding row. A direct `hhid2` join would pair the index with another household's outcome. However, all 5,605 local rows align with the officially weighted Module A sample sorted by numeric `a01`, matching **every row on 13 shared Module A fields** and household size from the separate expenditure file. This establishes an auditable derived `a01`/corrected-`hhid2` mapping for this analysis, not a correction to the original source file. The linked sample has 64 official district codes, 275 official community IDs, and 5,604 nonmissing expenditure values. The official release's 5,604-household count versus the 5,605-row weighted sample still requires source documentation; neither count was silently changed.

**Status.** A separately measured contemporaneous welfare outcome is now available for Extension 2 criterion association and Extension 3 held-out-location comparison. FIES remains unavailable. The newly discovered key error materially revises the earlier data-availability assessment; welfare validation proceeds next using only the audited derived mapping.

## 2026-09-24 — Extension 2 follow-up: Robust-High welfare criterion association

**Work done.** Created and ran `outputs/extension2_robust_high/validate_welfare.py` on the audited public welfare link. Compared the strict robust-High, never-High, switching, and three incomplete/uncertified records on separately prepared monthly per-capita household expenditure. Estimated the robust-versus-never difference in mean log expenditure with 2,000 whole-district bootstrap resamples and saved `welfare_by_robustness.csv` and `welfare_criterion_summary.json`. This replaces the earlier Task 1.2 “no outcome available” status for the **welfare** criterion only; the FIES outcome remains unavailable.

**Findings.** Among 5,604 nonmissing expenditure records, strict robust High has n=705, median monthly per-capita expenditure **3,413.95**; never High has n=2,543, median **4,303.25**; switchers have n=2,353, median **3,934.28**. The robust-minus-never mean log-expenditure difference is **−0.1948** (district-bootstrap 95% interval **−0.2733 to −0.1077**), a geometric-mean ratio of **0.823**. The continuous LVI has Spearman correlation **−0.1259** with log expenditure. These are cross-sectional criterion associations using a separately prepared welfare aggregate; the index contains income and meal indicators, so conceptual overlap remains. No temporal, causal, survey-representative, or food-insecurity classification claim follows.

**Status.** Extension 2 now has an independent-measure welfare association conditional on the audited derived key map. Extension 3 held-out-district baseline comparison is next.

## 2026-09-24 — Extension 3 follow-up: Held-out-district welfare comparison

**Work done.** Created and ran `outputs/extension3_outcome_validation/evaluate_welfare.py` on the audited public-source link. Used five-fold GroupKFold over all **64 official Module A district codes**, holding each district wholly out of model fitting. On the same 5,604 households with nonmissing separate expenditure, compared the training-mean log-expenditure baseline, LVI-only ridge model, demographic baseline (head age, sex, household size), demographic-plus-LVI, and demographic-plus-strict-robust-High. Saved row-level out-of-fold predictions, pooled metrics, district-level paired MAE differences, and 2,000-resample district-bootstrap intervals. No FIES or future outcome was substituted.

**Results.** Pooled log-scale MAE / R² are: training mean **0.3723 / −0.0084**; LVI only **0.3683 / 0.0113**; demographic baseline **0.3578 / 0.0733**; demographics plus LVI **0.3542 / 0.0879**; demographics plus robust High **0.3566 / 0.0779**. The equal-district mean MAE increment of adding LVI to demographics is **−0.00237** (district-bootstrap 95% interval **−0.00606 to 0.00148**, 44/64 districts improved); adding robust High is **−0.00065** (interval **−0.00243 to 0.00131**, 38/64 improved). Lower MAE is better; both intervals include zero. Thus the index gives at most a small, uncertain incremental improvement for this contemporaneous welfare aggregate, and the robust-High flag adds little beyond simple demographics in this design.

**Scope.** This is cross-sectional criterion association and held-out-district transfer conditional on the archived full-sample index and audited positional key repair. It does not validate FIES, future food security, climate timing, a causal effect, population inference, or operational use. The original `hhid2` column remains unsafe for direct outcome linkage.

## 2026-09-24 — Extension results documentation and supersession

**Work done.** Added `paper/15_extension_welfare_results.md` with the public source, 14/14 exact row-alignment checks, corrected-key mapping, Extension 2 welfare association, Extension 3 baseline comparison, and interpretation limits. Added prominent supersession notices to the earlier outcome-availability, X5 linking, validation-status, and Step 1 provenance notes so readers do not treat the prior “no welfare outcome available” or “one extra unkeyed household” interpretations as current findings. The original cleaned data and existing model artifacts remain unchanged.

**Current findings.** The external welfare file provides 5,604 nonmissing matched expenditure outcomes after an audited `a01`-order correction. The robust-High group has lower median expenditure and a negative district-bootstrap log-expenditure contrast relative to never High. In held-out-district evaluation, continuous LVI gives a small, uncertain incremental improvement over demographics; the robust-High flag adds little. Female X5/FIES data remain absent. The official report's 5,604 households versus 5,605 weighted source rows remains a source-definition discrepancy separate from the now-explained local `hhid2` misalignment.

**Status.** Both requested extensions now have reproducible outputs for the available welfare criterion. Food-security-specific and temporal validation remain unavailable; no such result is claimed.

## 2026-09-24 — Extension results integrated into the manuscript

**Work done.** Updated the authoritative `paper/main.tex`, synchronized `paper/07_paper_draft.md`, and added the public World Bank archive citation to `paper/references.bib`. Corrected the manuscript's stale missing-ID/extra-row interpretation using the 14 exact source-row checks; added the strict all-11 robust-High definition and one-at-a-time switch drivers; added the separately measured welfare criterion, five-fold 64-district comparison, and a new Table IV tied to saved metrics. Revised the abstract, limitations, conclusion, and future-work boundary to distinguish welfare association from unavailable FIES or future-outcome validation. Rebuilt with `pdflatex`/`bibtex`/two `pdflatex` passes and checked rendered table text.

**Verification.** The six-page A4 PDF has no undefined citations/references or overfull boxes; the revised abstract is 152 source words. Table IV values match `welfare_baseline_comparison.csv`, and the reported robust-High contrast and district intervals match the saved welfare summaries. The paper now reports the small, uncertain incremental welfare gain and explicitly labels full-sample index scaling as transductive. The original cleaned CSV and prior experimental artifacts were not altered.

**Status.** The requested Extension 2 and 3 manuscript components are complete for the public same-wave welfare criterion. The female X5/FIES module and later outcomes remain unavailable, and the IFPRI 5,604-versus-weighted-source 5,605 count still needs source-definition reconciliation.
